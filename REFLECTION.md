# MuseEcho 项目反思

## 我做了什么、为什么有人会用

本项目完成的是 MuseEcho——一个以“证据优先”为核心理念的交互式音乐理解应用，面向希望理解一首音乐的节奏、调性、和弦、结构变化与情绪走向的用户。传统播放器主要提供播放和进度控制；MuseEcho 将音乐分析结果转化为可以观察、定位和追问的过程，使用户不仅得到结论，也能理解其对应片段和依据。

MuseEcho 将音频分析、统一时间轴、和弦信息、结构分段、能量变化和 Evidence Q&A 结合起来。用户上传音频后，系统通过 DSP/MIR 分析生成节拍、能量、调性、结构和和弦等音乐事实，再将结果展示在时间轴和交互界面中。用户可以结合播放进度查看不同片段的音乐特征，也可以选择片段提问，获得基于已有证据的解释。

项目最重要的边界是：MuseEcho 不把 LLM 当作“猜测音乐事实”的工具。节拍、和弦、调性和结构等事实由确定性的音频分析管线产生；LLM 只在证据、置信度和引用范围受到约束的前提下进行自然语言解释。当证据不足、置信度不够或输出不符合边界时，系统回退到确定性的解释，而不是让模型生成无法验证的音乐结论。

后续如果继续迭代 MuseEcho，我认为可以训练或接入更适合音乐理解任务的分析模型，提高对复杂和声、曲式结构、音色和音乐情绪的识别能力；也可以结合用户反馈增强系统的学习能力，逐步改善解释方式和内容呈现。进一步还可以增加社区功能，让用户分享分析结果、片段标注和音乐理解，但这需要同时完善版权、隐私和内容治理机制。

## 我实际检查过什么

在项目实施过程中，我没有只根据智能体的“完成”描述判断结果，而是对主要流程进行了人工检查。我手动上传了自己有权使用的音频，查看上传、格式校验、分析等待、结果展示和交互过程，并检查 Music DNA、时间轴、和弦详情、Evidence Q&A、再次上传及删除等核心功能能否形成完整流程。

我也检查了前端页面的设计与信息呈现，包括中文文案、结果层级、时间轴交互、状态提示和不同尺寸下的可读性。发现疑问或不符合预期的地方时，我不会直接接受智能体的解释，而是要求其复现、补充测试、重新审查，并据此调整后续工作方向。

在工程过程方面，我检查了 GitHub 仓库的提交、分支与合并记录，确认项目没有通过一次性提交掩盖开发过程；同时关注各次 GitHub CI 的执行结果，并根据失败信息引导智能体排查问题、补充测试或修正实现。对于凭据安全，我检查了项目关于 Secret 管理和扫描的边界，确认真实密钥不应进入源码、Git 历史、日志、截图或普通配置文件。

需要如实说明的是，GitLab 本轮 Pipeline、腾讯云公网部署、受信任 TLS、目标服务器验证、24 小时观察、备份恢复和真实回滚仍未执行。根据教师允许仅使用 GitHub 仓库并将腾讯云部署转为后续计划的最新要求，这些事项不作为本次课程提交门禁，但不能被写成已经完成。项目当前仍是 `MUSEECHO V1 PARTIALLY READY`，因为最终 GitHub 证据、正式离线构建、产品人工审查和学生最终验收仍需如实闭环。

## AI 与 Superpowers 工作流的真实得失

我认为，遵循 Superpowers 的既定流程在本项目中起到了明显的约束作用。brainstorming、SPEC、PLAN、cold-start、TDD、代码审查和交付审计虽然增加了过程成本，但也避免了许多前后矛盾，使项目能够较有序地推进。

一个典型例子是项目最初关于 LLM 边界的表述。“LLM 不产生音乐事实”容易被理解为系统不再提供和弦、调性或结构分析功能。经过我的质询和后续修订，项目才明确为：音乐事实仍由 DSP/MIR 或专用分析能力生成，LLM 只解释已有证据。这说明 AI 的表述即使看似合理，也可能存在理解歧义，人工必须参与澄清。

另一个例子是音频生命周期。最初“分析后立即删除音频”的方案，与用户刷新页面后仍希望播放和定位片段的需求发生冲突。经过讨论，项目改为音频在限定期限内加密保留，并支持主动删除和到期清理。这一变化影响了数据模型、音频访问方式、密钥生命周期和测试范围，说明完整流程能提前发现需求矛盾。

Superpowers 也提高了 AI 协作的稳定性。AI 有明确的工作范围、操作边界和验证要求，即使沟通中存在遗漏，也不容易直接进行高风险或缺乏证据的操作。特别是在安全、分发、CI 和部署任务中，智能体不能仅凭“看起来正确”就宣布完成，而需要形成由测试、命令结果、审计材料和过程记录组成的证据链。

但这套流程也有不足。对于较简单的任务，它可能拆分得过细，使规划、记录、分支、审查和验证所花费的时间超过实现本身。完整测试、容器验证、安全扫描和审计耗时较长；边界过于死板时，AI 也容易在同一类形式问题上反复出错。更需要警惕的是，AI 有时会过度关注显式的测试、文档格式或 checker 规则，而错过真正影响用户体验或工程质量的问题。因此人工的价值仍然在于判断什么问题更重要，不能只把“测试通过”视为唯一目标。

## TDD、subagent 与人工评审

TDD 和审计在 Task 20、Task 23 中给我留下了较深印象。Task 20 涉及生产容器、CI、Secret 审计和镜像安全。审计发现，应用镜像的原始漏洞扫描存在大量没有上游修复版本的 HIGH/CRITICAL 条目。项目没有简单地把扫描结果包装成“安全通过”，而是保留原始扫描事实，并要求镜像身份、组件清单、执行边界和 VEX 结论之间能够相互对应。

正式的离线 Docker 构建仍因 BuildKit 缓存条件不足而没有完成，这一问题不能通过文档措辞掩盖，因此被保留为 `ENG-010 BLOCKED`。这使我认识到，人工评审的重要作用不是让 AI 给出更漂亮的结论，而是阻止它在证据不足时过早宣称成功。

Task 23 的审查也体现了测试驱动和复审的必要性。审查中发现，`.gitignore` 并不等于 `.dockerignore`，未被 Git 跟踪的生成文件仍可能进入 Docker 构建上下文。针对这一问题，先通过失败测试暴露边界缺口，再修正 Docker 上下文规则和相关测试，最后重新验证构建材料与审计结果。这个例子说明，TDD 不仅适用于业务功能，也适用于安全边界、发布材料和证据真实性。

subagent 能提高实现速度，但不能代替人工负责。不同 Agent 的 cold-start 尝试曾暴露 SQLite 外键、仓储实现、UTC 时间处理和领域状态约束等问题。这说明另一个智能体能够依据 SPEC 和 PLAN 开始实现，不代表规格已经足够清楚或实现已经达到工程要求；人工仍需检查 AI 的假设、测试范围和完成标准。

## 如果重新做一次

如果重新开展这个项目，我会在初始提示词中明确限制 AI 频繁读取运行状态或重复进行低价值检查，以节省 token 和时间；同时要求 AI 以阶段性反馈文档、重大错误日志和待决问题清单汇报进展，减少长链路任务中的信息遗漏，并使人工能够更早发现方向偏差。

我也会更早确认 NJU GitLab、镜像 registry、腾讯云账号、域名、DNS、SSH 权限和构建缓存等外部前置条件。项目后期的部分阻塞并非代码没有实现，而是外部环境和授权条件尚未具备。若这些条件在中期就明确，部署、CI 和发布相关工作可以更早安排，而不会集中在最终交付阶段。

此外，我会按风险等级设计任务粒度：核心算法、安全边界、部署和数据生命周期等高风险任务继续细拆；简单文档修改或局部 UI 调整则适当合并，避免流程本身成为效率负担。测试方面也应区分快速反馈和完整门禁：开发时优先运行聚焦测试，合并和交付前再执行容器、E2E、扫描和审计等重型验证。

最后，我会更早安排人工产品体验验证。自动化 E2E 能证明一部分实现边界，但不能完全替代用户在可信 TLS、真实网络和不同设备上的体验。未来我会更早检查首次使用引导、分析等待、信息理解、错误恢复、移动端可读性和隐私提示，避免最后阶段才发现自动化测试无法覆盖的产品问题。

---

写作说明：本文反思的事实内容由本人基于项目过程和实际检查整理；AI 仅用于协助语言组织与表达润色。
