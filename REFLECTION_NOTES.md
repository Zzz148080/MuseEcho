# MuseEcho 反思笔记

本文件仅积累客观过程材料，不代写学生的 `REFLECTION.md`，也不替学生生成主观结论。

<!-- TASK23-CURRENT-STATUS:START -->
## 任务 23 兼容状态（已由任务 24 取代）

功能审计为 **31 PASS / 9 PARTIAL / 0 FAIL**，结论为
`PARTIALLY_READY`。GitHub `quality`、E2E 和 `distribution` 通过后，任务 23 PR #1 已合并。
其实现边界在此继续作为任务 23 回归合约保留；当前分支顶点和权威状态见下方任务 24 状态块。
任务 23 收尾时的阻因键为 `GitLab`、`TC-021`、`TASK24-AUDIT`、`STUDENT-MANUAL`
和 `FORMAL-OFFLINE-BUILD`。
<!-- TASK23-CURRENT-STATUS:END -->

<!-- TASK24-CURRENT-STATUS:START -->
## 任务 24 当前状态

当前状态为 `MUSEECHO V1 PARTIALLY READY`。任务 24 的产品审计、固定 17 节的
交付报告、校验器和测试均已完成，任务 24 不再是阻塞项。任务 23 PR #1 已合并，
GitHub 三个作业均为绿色。历史任务 24 运行 `31687703913` 与最终产品实现运行 `31966788273`
继续保留各自日期对应的边界。PR #3 已合入 main SHA
`d99e7b95f83f0f5cd6867bd10bacc274e6d2a0e1`；main run `31997390847` 的
`quality`、E2E 和 `distribution` 均通过，正式 GitHub Release `v0.1.0` 也已发布四项经校验和
验证的离线资产。根据 `COURSE_REQUIREMENT_UPDATE.md`，GitLab 与腾讯云/公网部署属于后续工作。
正式当前源码离线重建 `ENG-010`、控制器浏览器产品复核和学生门禁仍未完成。以下陈述按
时间从早到晚排列。
<!-- TASK24-CURRENT-STATUS:END -->

## 2026-08-08 — 前置设计阶段

- 头脑风暴的一次一问机制产生了连续的真实用户选择，没有用 AI 自行模拟批准。
- 视觉伴侣用于比较三种布局和三种视觉方向；终端选择与浏览器点击记录一致。
- “LLM 永远不产生事实”被用户理解为“这些功能不做”。经质询后改写为 DSP/MIR 产生事实、LLM 解释证据。这是一次由 SPEC 措辞造成的真实误解案例。
- 最初的“分析后立即删除音频”与刷新后播放冲突。用户在看到三种方案后改选 24 小时加密保留，导致数据模型、Range API、密钥生命周期和测试范围扩大。
- Fly.io 的大陆访问和付款问题使初始部署建议失效。AutoDL 又因个人公网端口和第三方访问条款不满足课程公网 WebUI。最终选定腾讯云香港 Lighthouse。
- 此阶段尚未执行 TDD、子智能体、工作树、CI 或代码审查；不得提前评价它们的效果。
- 现有工作区没有 Git 历史，必须从设计文档开始建立真实的细粒度历史。

## 2026-08-11 — 任务 21 / 交付边界的本地证据

- 任务 20 的 tar/配置身份不能被叙述成已发布的镜像仓库摘要；任务 21 因而明确拒绝标签，要求部署操作员提供真实 `name@sha256:` 引用。
- 第一次脚本集成测试暴露测试根目录的非 root 身份不能模拟生产 `chown`，因此测试适配器只在 `MUSEECHO_TEST_ROOT` 下省略所有者切换；真实路径仍使用 root 与 GID 10001。另一个 RED 暴露 systemd 只读取运行时环境却没有发行镜像变量，且第三方提供方密钥路径被无条件配置；修复后每个不可变发行版本都有非秘密 `release.env`，默认仅 KEK 启动时第三方提供方三项配置保持全空。
- 外部授权缺失不是停止可验证本地工作的理由。`DEPLOYMENT_EVIDENCE.md` 把脚本和临时根目录证据与尚未发生的公网冒烟、跨网测试、清理和回滚演练明确分开。

## 2026-08-11 — 任务 22 / 证据驱动验收材料

- 单纯列出文件无法证明验收通过。任务 22 将 24 个 AC 和 16 个 DoD 条目固定为 40 个 ID，并让校验器拒绝缺失/重复、文件存在型 PASS、无命令/路径/UTC/退出码证据、未来时间和阻塞项/READY 矛盾。
- 当前锁定 Linux 运行时没有 Git。校验器首版把“精确历史提交可验证”误实现为“任何环境都必须现场启动 Git”，导致 612 个既有测试通过而 23 个新测试同因 `FileNotFoundError` 失败。修复后的边界是精确 40 位提交与命令/路径绑定；有 Git 时额外验证对象，没有 Git 时仍能运行离线矩阵门。
- PowerShell 会把子进程错误记录（ErrorRecord）按显示宽度换行。密钥扫描器已正确失败关闭，但合成测试框架把格式化文本当原始字符串匹配而误报；对诊断输出先规范空白即可保持完整文件名断言，不需要降低扫描标准。
- 本地两核五分钟基准、内部 CA 冒烟和配置文件存在不能替代目标服务器、公网受信 TLS 或远程 CI。任务 23 复审后的当前矩阵为 `28 PASS / 12 PARTIAL / 0 FAIL`；前端类型检查/构建在本轮未运行即不得沿用任务 22 PASS，且学生人工验收继续保留为人工待办。
- 容器冒烟的强制构建在缓存失效时执行了锁定 `npm ci`。虽然锁文件和依赖版本没有变化，这仍是一次与任务 22 禁止下载约束不一致的过程事件，必须保留在报告而不能用“可复现构建”掩盖。
- 退出码 0 只是进程结果，不是功能证据。审查变异测试证明，把当前证据的命令和结果一起改成无意义成功仍可绕过初版校验器；代码内逐项固定证据命令、覆盖范围和可量化结果后，审计文本本身不能同步改写策略。
- 无 Git 可移植性不能以放弃真实性为代价。历史 E2E 现在同时绑定 PLAN 中的完整提交锚点、历史内容摘要和当前源码/测试 SHA-256 清单；有 Git 时再用单次归档校验历史清单。这样无 Git 运行时能拒绝只改审计文本的伪造，同时不要求生产镜像安装 Git。
- “过去跑过浏览器测试”只有在相关边界未变时才能支撑当前 PASS。任务 19 与当前清单的客观漂移使 5 个条目从 PASS 降为 PARTIAL；这比用历史成功填满矩阵更准确，也保留了在合适隔离环境重跑的明确闭环。
- README 与 CI 文件存在不等于交付可用。当前 E005 通过解析两套 CI（包括 GitLab `unit-test`）并断言 README 的锁定安装、开发 HTTPS、生产冒烟、健康检查与清理路径，把 AC-F-2/AC-F-3/DOD-11/DOD-12 绑定到可测合约而不是 `git show --stat`。

## 2026-08-11 — 任务 23 / 工程审计过程材料

- 多文件 CLI 的“退出码 0”不能证明每个输入都被消费。Bash `-n` 只解析第一个脚本的行为说明，变异必须放在最后一个输入才能暴露这种覆盖缺口；修复应逐文件建立独立进程边界。
- 清理不是附属成功路径。部分启动后必须进入清理，且主失败与清理失败是两个独立事实；只保留任一都会让运维诊断失真。
- “no-build”不仅是不运行构建，还要在启动前确认精确的本地镜像身份，并让 Compose 显式执行 `up --no-build`。这使离线审计不会因缓存未命中而意外进入依赖下载。
- 完整运行时清单揭示了被 Git 忽略的 egg-info 被 Docker 构建上下文带入任务 20 镜像/策略。`.gitignore` 不等于 `.dockerignore`；干净检出的可复现性必须从真实 Docker 构建上下文和镜像内容验证，而不是从已跟踪文件列表推断。
- VEX 可以在原始 181/67 不变时得到剩余项为零，但前提是每个 CVE 的包/文件/可达性证据、完整源码/运行时摘要与精确镜像身份均未漂移。本轮可观测性变更不增加解码器、FFmpeg、动态 SQL、子进程或外部执行路径，因此保留原 67 条声明；先观察到的边界漂移被明确当作 RED，而非自动刷新掩盖。
- 大型安全证据不必全量进入 Git，但紧凑清单必须是确定性的、由校验器固定并包含足够交叉字段。单独的“文件存在”或 Markdown 中的“0 个发现项”仍不构成验证。
- 宿主缺 FFmpeg 导致基准失败时，应在实际锁定 Linux 镜像中复验；本轮同一五分钟测试在镜像内 51.24 秒通过。相同原则适用于缺 `pwsh`、`uv`、`@types/node` 或 Playwright 缓存：记录精确环境限制，不下载修环境，也不把历史成功伪装成本轮执行。
- 复审还说明“证据内部自洽”并不等于证据独立。FIXED 发现项必须绑定它自己的 RED/GREEN 命令、路径、结果和覆盖；no-build 必须绑定可信身份并检查实际运行容器；历史前端构建也不能在本轮未执行时继续支持 PASS。
- 任务 23 第二轮复审进一步证明紧凑清单自身固定仍不足以构成完成：默认校验器必须实际读取并复算保留的原始扫描/包/VEX/清单/tar/发行/数据库/镜像材料；无材料的便携校验只能显式称为 `--schema-only`。正式离线 Dockerfile 构建失败也必须成为独立 BLOCKED 发现项，而不能只藏在报告关注项中。

## 2026-08-13 — 任务 24 / 客观交付材料

- 交付报告的 17 节来自状态摘要加 README 交付合同的 16 类，不把 SPEC 的 24 章误称为用户要求的 17 节。
- 产品审计表完整覆盖 13 个体验域；控制器真实启动了 no-build HTTPS 服务并观察到 API 已就绪，但浏览器在渲染前拒绝内部 CA。自动测试不能替代未发生的视觉观察，因此统一保留 `CERT_TRUST_BLOCKED`。
- `PARTIALLY READY` 不是模糊结论。每个阻塞项都有负责人、待补证据和可执行关闭条件；任务 24 制品完成后不再把自身留作阻塞项。
- 学生反思的真实性边界由空白模板与校验器双重保护：学生核对项不能被智能体勾选，反思正文和签名保持空白。

## 2026-08-17 — v0.1.0 离线运行发行客观材料

- PR #3 合并为 `main` SHA `d99e7b95f83f0f5cd6867bd10bacc274e6d2a0e1`；`main` 运行 `31997390847` 的 `quality`、E2E、`distribution` 全部成功。Actions 配额只跳过工作流制品上传，不改变各作业的执行结论。
- 正式 GitHub Release `v0.1.0` 于 `2026-08-17T05:54:50Z` 发布，非草稿、非预发布，标签精确指向该 `main` SHA，并恰好包含两个镜像 tar、离线运行时 zip 与 `SHA256SUMS.txt`。
- 四项资产发布后均从 GitHub 实际回下载并重新计算 SHA-256；结果与发行清单完全一致。解压包完成 `Verify`、导入、清单身份、no-build HTTPS/WAV、重启持久化、无持久化明文音频与清理 Smoke。
- 该 Release 关闭离线运行发行，不关闭 `ENG-010` 的当前源码断网重建，也不产生公开 OCI 镜像仓库、GitLab、腾讯云部署、受信公网 TLS、目标服务器或学生验收证据。
